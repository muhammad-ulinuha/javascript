var password = true;
while (password) {
    var pass = prompt('Masukkan Password : ');
    if (pass != 'ulin') {
        alert('password salah');
    } else {
        password = alert('password benar');
    }
    
}
